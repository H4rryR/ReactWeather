var React = require('react');
var {Link} = require('react-router');

var Grid = (props) => {
    return (
        <div>
            <h1 className="text-center page-title">Grid</h1>
            <p className="text-center"><em>The grid is currently under construction!</em></p>
        </div>
    );
}

module.exports = Grid;
